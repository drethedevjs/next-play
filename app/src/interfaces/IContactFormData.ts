export default interface IContactFormData {
  firstName: string;
  lastName: string;
  email: string;
  message: string;
  city: string;
  region: string;
  postalCode: string;
  services: string[];
  sports: string[];
  company: string;
}